node_modules
.next
.env
.env.local
*.log
.DS_Store

# Prisma local SQLite DB — contains seeded password hashes; never commit.
prisma/dev.db
prisma/*.db-journal
prisma/migrations/dev.db*

# Build cache
.next/cache

# Editor
.vscode
.idea

# OS
Thumbs.db
.vercel
.env*.local

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# gstack local state + security reports
# `/cso` writes findings to .gstack/security-reports/{date}.json — those
# files contain verbatim exploit scenarios and must NOT be committed.
.gstack/
.gstack/
